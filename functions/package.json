{
  "name": "chopcircle-functions",
  "description": "ChopCircle Cloud Functions (Phase 11+ scaffold — see README.md before deploying)",
  "private": true,
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions,firestore,auth,storage",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
